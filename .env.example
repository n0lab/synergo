# Synergo Docker Configuration
# Copy this file to .env and modify as needed

# =============================================================================
# HOST CONFIGURATION
# =============================================================================

# Port to expose the application on your host machine
# Default: 42001
PORT=42001

# =============================================================================
# DATA PERSISTENCE (Host paths)
# =============================================================================

# Path on the host machine where the database will be stored
# This ensures your data persists when the container is restarted
# Default: ./data (relative to docker-compose.yml location)
DATA_PATH=/volume1/docker/synergo/data

# Path on the host machine where media files (videos, photos) will be stored
# Default: ./resources (relative to docker-compose.yml location)
RESOURCES_PATH=/volume1/docker/synergo/resources
